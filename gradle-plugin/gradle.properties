# Gradle supplies the Kotlin standard library to the plugins it loads, so the
# published tlaloc-gradle-plugin does not declare one (build.gradle.kts adds it
# compileOnly).
kotlin.stdlib.default.dependency=false
